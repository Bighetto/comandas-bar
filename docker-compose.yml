version: “3.7”
services:
  projeto-bar:
    image: openjdk:11
    ports:
      - "8080:8080"
    volumes:
      - ./app:/usr/src/app
      - ./target/comandas-bar-1.0.jar:/app/bar.jar
    working_dir: /app
    environment:
      IP: mysql
      USERNAME_DB: root
      DATABASE: projetoBar
      PASSWORD_DB: teste
    command: java -jar bar.jar
    depends_on:
      - mysql
    networks:
      - projetobar
  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: teste
      MYSQL_DATABASE: projetoBar
    ports:
      - "3306:3306"
    restart: always
    networks:
      - projetobar
    volumes:
      - ./data:/var/lib/mysql
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
networks:
  projetobar:
    driver: bridge



